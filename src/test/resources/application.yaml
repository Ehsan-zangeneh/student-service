spring:
  datasource:
    url: jdbc:h2:mem:testdb;
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


logging:
  level:
    root: INFO            #Entire application
    org.springframework.web: INFO
    com.student.sample: DEBUG    #All classes here
